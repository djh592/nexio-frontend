import { Box } from '@mui/material';

export default function ChatPage() {
    return (
        <Box
            sx={{
                height: '100%',
                width: '100%',
            }}
        >
            <h1>Chat Page</h1>
        </Box>
    );
}