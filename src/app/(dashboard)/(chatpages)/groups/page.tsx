import { Box } from '@mui/material';

export default function GroupsPage() {
    return (
        <Box
            sx={{
                height: '100%',
                width: '100%',
            }}
        >
            <h1>Groups Page</h1>
        </Box>
    );
}