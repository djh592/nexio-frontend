import { Box } from '@mui/material';

export default function AccountPage() {
    return (
        <Box
            sx={{
                height: '100%',
                width: '100%',
            }}
        >
            <h1>Account Page</h1>
        </Box>
    );
}